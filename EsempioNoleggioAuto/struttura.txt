NOLEGGIO AUTO:

ENTITY:
- Auto = String targa, String marca, String modello, int num_posti, TipoAuto tipoAuto, StatoPrenotazione statoPrenotazione
- Cliente = int id, String nome, String cognome, String numeroTelefono, String email


- Noleggio = private Map<Auto, Cliente> noleggi = new HashMap<>();

ENUM:
TipoAuto = MONOVOLUME, STATION_WAGON, UTILITARIA
StatoPrenotazione = DISPONIBILE, NON_DISPONIBILE, NULLO

INTERFACCE:
- AutoService = aggiungiAuto (Auto auto), Auto trovaAuto (String targa), List<Auto> trovaAuto (String modello), eliminaAuto (String targa), listaAuto (StatoPrenotazione stato),
- ClienteService = aggiungiCliente (Cliente cliente), trovaCliente (int id), eliminaCliente (int id), listaClienti (tutti i clienti)
- NoleggioService = aggiungiNoleggio (Cliente cliente, Auto auto), trovaNoleggio (Cliente cliente), eliminaNoleggio (Cliente cliente), listaNoleggi (tutti i noleggi)

- AutoServiceImpl
- ClienteServiceImpl
- NoleggioServiceImpl


<bean id="clienteServiceImpl">
<bean id="autoServiceImpl">
<